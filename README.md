# crypt-basic-plus

**Goal:** Fix the small issues so running `python main.py` prints the secret message:

EXPLORE, CREATE, INSPIRE.

css
Copy code

Slight hint (small, fair nudge):

The secret file is Base64; watch out for str vs bytes and trailing whitespace/newlines when decoding.


**How to run**
```bash
python main.py

